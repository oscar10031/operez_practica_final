pipeline {
    agent any
    stages {
        stage('Creación de la red docker') {
            steps {
                sh "docker network create red-operez"
            }
                }
            }

        }
